<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.arrived.arrived">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:logo="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/arrivedThemeV3">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GettingStarted" />
        <activity android:name=".VisaHelp" />
        <activity android:name=".Housing" />
        <activity android:name=".CareerGuidance" />
        <activity android:name=".LegalHelp" />
        <activity android:name=".DeportationHelp" />
        <activity android:name=".EnglishHelp" />
        <activity android:name=".SupportGroups" />
        <activity android:name=".FAQ" />
        <activity android:name=".AboutUs" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps" />
        <activity android:name=".menu" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".driving" />
        <activity android:name=".financialAdvice" />
        <activity android:name=".healthKnowledge" />
        <activity android:name=".youTube"></activity>
    </application>

</manifest>